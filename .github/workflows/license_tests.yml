name: Run License Tests
on:
  push:
  workflow_dispatch:

jobs:
  license-tests:
    uses: neongeckocom/.github/.github/workflows/license_tests.yml@FEAT_CommonWorkflows
    with:
      package-extras: docker,pi